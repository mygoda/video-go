# 复制成 .env.local 后按需修改。这里不放任何真实凭证 —— 模型 Key 由后端统一持有。
#
# 注意：.env.local 的优先级高于代码里的默认值。本机若曾写过 VITE_USE_MOCK=true，
# 前端就会一直跑假后端，跟代码默认值无关。排查“数据怎么还是假的”先看这个文件。

# 离线开发模式开关。**不配置时走真后端**。
#   true           = 用 src/mock/ 的浏览器内假后端，不启 Go 后端与 MySQL 也能跑通 UI；
#                    数据全部是 src/mock/db.ts 的硬编码假数据
#   不配置 / 其他值 = 走真后端（VITE_API_BASE）
# 假后端只能这样显式打开，不会再被隐式启用。
# VITE_USE_MOCK=true

# 真后端基址。默认 /api，由 vite dev server 代理到 VITE_API_PROXY_TARGET
VITE_API_BASE=/api

# dev server 代理目标（仅走真后端时生效）。后端需以 AIGC_HTTP_ADDR=:18080 启动
VITE_API_PROXY_TARGET=http://localhost:18080
